#include "main.h"

/**
  * mul - multiplies two integers
  * @a: one of the characters to be multiplied
  * @b: the other character to be multiplied by
  * Return: multiplication value
  */

int mul(int a, int b)
{
	return (a * b);
}
