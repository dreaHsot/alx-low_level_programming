#include "main.h"

/**
 * _abs - retuns the absolute value of an integer
 * @n: the integer to be absolute
 * Return: absolute value of n
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
